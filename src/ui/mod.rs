//! UI screens. Each submodule renders one screen with the egui 0.36 API
//! (`App::ui` hands the app a root `&mut egui::Ui`).

pub mod activity;
pub mod addresses;
pub mod capabilities;
pub mod contracts;
pub mod onboarding;
pub mod theme;
